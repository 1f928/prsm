Themes will be of the pattern:

```js
const exampleTheme = {
    light: {
        "--variable-name": "variable value", // e.g. :
        "--main-color": "#2c002e",
    },
    dark: {
        // same variables as above, potentially different values
    }
}
```